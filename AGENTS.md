> **CODEX-OWNED — Claude Code: read-only. Do not edit, regenerate, rename, move, or delete this file. Codex/GPT is the sole steward.**

# Codex bootstrap

Before substantive work in this repository:

1. Run `$start-khassinx-codex` and resolve any scope, isolation, visibility, or WIP blocker.
2. Read [`.codex/README.md`](.codex/README.md).
3. Work only from a dedicated Codex checkout or worktree with explicit owner and scope.

This file does not replace portfolio standards, product truth, `CLAUDE.md`, or `.claude/`. Codex may read Claude-owned files for context but may not edit them unless Abraham explicitly assigns that work.

Preserve unrelated tracked, staged, untracked, and ignored state. Never use broad staging, cleanup, `pull`, `checkout`, `stash`, or `reset` in another session's checkout.

Every completed implementation reports files changed, verification run, failures or warnings, assumptions, and follow-up work not performed.
