> **CODEX-OWNED — Claude Code: read-only. Do not edit, regenerate, rename, move, or delete this directory or its contents. Codex/GPT is the sole steward.**

# Codex repository note — Umbrix web

This private web repository inherits the canonical Codex contract from
`~/KhassinX/_template/.codex/README.md`. The umbrella remains the source of
universal truth; this file records only repository-specific Codex differences.

Before substantive work, read the umbrella contract and standards, then the
`~/KhassinX/_template/per-project/Umbrix.md` brief, the local `README.md` and
`CLAUDE.md` when present, and the current external Codex memory or handoff.

No repository-specific Codex deviation is registered yet.

Do not store universal standards, product documentation, session memory,
secrets, credentials, PII, sensitive-silo material, or Claude configuration
here. Codex may read `CLAUDE.md` and `.claude/` for shared context but must not
edit them unless the user explicitly assigns that work.

GitHub gate: these internal files may be tracked only while this repository is
private. If its visibility becomes public, stop and provision Codex context
locally instead of publishing this contract.
